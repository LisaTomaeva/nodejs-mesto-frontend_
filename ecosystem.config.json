{
  "apps": [],
  "deploy": {
    "production": {
      "user": "$DEPLOY_USER",
      "host": ["$DEPLOY_HOST"],
      "ref": "$DEPLOY_REF",
      "repo": "$DEPLOY_REPO",
      "path": "$DEPLOY_PATH",
      "post-deploy": "npm install && npm run build"
    }
  }
}